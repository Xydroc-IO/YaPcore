diff --git a/folia-server/build.gradle.kts.patch b/folia-server/build.gradle.kts.patch
index 354dcc7..285ddab 100644
--- a/folia-server/build.gradle.kts.patch
+++ b/folia-server/build.gradle.kts.patch
@@ -57,17 +57,17 @@
          attributes(
              "Main-Class" to "org.bukkit.craftbukkit.Main",
 -            "Implementation-Title" to "Paper",
-+            "Implementation-Title" to "Folia",
++            "Implementation-Title" to "YaP-Folia",
              "Implementation-Version" to implementationVersion,
              "Implementation-Vendor" to date,
 -            "Specification-Title" to "Paper",
-+            "Specification-Title" to "Folia",
++            "Specification-Title" to "YaP-Folia",
              "Specification-Version" to project.version,
              "Specification-Vendor" to "Paper Team",
 -            "Brand-Id" to "papermc:paper",
 -            "Brand-Name" to "Paper",
-+            "Brand-Id" to "papermc:folia",
-+            "Brand-Name" to "Folia",
++            "Brand-Id" to "yaplabs:yap-folia",
++            "Brand-Name" to "YaP-Folia",
              "Build-Number" to (build ?: ""),
              "Build-Time" to buildTime.toString(),
              "Git-Branch" to gitBranch,
diff --git a/folia-server/paper-patches/features/0002-Build-changes.patch b/folia-server/paper-patches/features/0002-Build-changes.patch
index 8b55c78..680b30d 100644
--- a/folia-server/paper-patches/features/0002-Build-changes.patch
+++ b/folia-server/paper-patches/features/0002-Build-changes.patch
@@ -82,10 +82,10 @@ index 74ffdc823e66fc5ec027c4b7c462382bcbfe2be2..10a0be7a4db1a51579d113d279af7a9e
              getManifestAttribute(manifest, ATTRIBUTE_BRAND_ID)
                  .map(Key::key)
 -                .orElse(BRAND_PAPER_ID),
-+                .orElse(Key.key("papermc", "folia")), // Folia
++                .orElse(Key.key("yaplabs", "yap-folia")), // YaP-Folia
              getManifestAttribute(manifest, ATTRIBUTE_BRAND_NAME)
 -                .orElse(BRAND_PAPER_NAME),
-+                .orElse("Folia"), // Folia
++                .orElse("YaP-Folia"), // YaP-Folia
              SharedConstants.getCurrentVersion().id(),
              SharedConstants.getCurrentVersion().name(),
              getManifestAttribute(manifest, ATTRIBUTE_BUILD_NUMBER)
