# Copy to .env next to docker-compose.yml (or rely on scripts which do this).
# Change YAP_DB_PASSWORD before exposing Postgres beyond localhost.

YAP_PG_PORT=5432
YAP_DB_NAME=yap_playerdata
YAP_DB_USER=yap
YAP_DB_PASSWORD=change-me
