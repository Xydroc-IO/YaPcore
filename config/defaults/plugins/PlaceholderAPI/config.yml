# YaP PlaceholderAPI — shipped defaults (typical SMP)
# Opt-in plugins: leave enabled: false until you want them
# Go public: see docs/start/SECRETS.md + DEFAULTS.md LAN vs public table
# Prefer dashboard: Plugin settings (or Gameplay tab) → Save → reload

# YaP PlaceholderAPI (clean-room, clip-compatible)
boolean:
  true: "yes"
  false: "no"
date-format: "MM/dd/yy HH:mm:ss"
# LAN-safe: no outbound update checks
check-updates: false
update-check-url: ""
cloud-enabled: false
dump-paste: true
expansions: {}
