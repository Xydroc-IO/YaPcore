server-id: default

dialogue:
  default: "&7Hello, traveler!"

# Packet nametag via YaPHolo (falls back to vanilla customName if Holo is off)
hologram-nametags: true
# Extra Y above entity height. Farmer hats auto-add more; raise this if names clip.
nametag-offset: 0.55

