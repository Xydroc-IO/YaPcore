# Shared MariaDB/MySQL for all YaP plugins that need SQL.
# Point every backend at the SAME database (Docker: deploy/mariadb/).

jdbc:
  # Packaged Docker MariaDB publishes on host 3316 (see deploy/mariadb/.env YAP_DB_PORT).
  url: jdbc:mysql://127.0.0.1:3316/yap_playerdata?useSSL=false&allowPublicKeyRetrieval=true
  user: yap
  password: change-me

pool:
  name: YaPDB
  maximum-pool-size: 16
  minimum-idle: 2
  connection-timeout-ms: 10000
