name: YaPWorld
version: '0.0.0.1'
main: com.yapcore.world.WorldPlugin
api-version: '1.21'
folia-supported: true
author: YapLabs
description: World management, region selection, and schematic tools (Folia-safe)
website: https://github.com/yaplabs
softdepend: [YaPPerms, YaPPregen, YaPProtect, YaPClaims, YaPPlayerData]
commands:
  yapworld:
    description: World edit + world admin (Folia-safe WorldEdit-class)
    usage: /yapworld help|gui|tool|set|copy|paste|resource|…  (also //set //copy …)
    # Per-subcommand permissions — do not require admin for basic editing
    aliases: [mv, worldedit, we]
permissions:
  yapworld.admin:
    default: op
  yapworld.resource:
    default: op
    description: List / force-reset resource worlds
  yapworld.load:
    default: op
  yapworld.create:
    default: op
    description: Create worlds with type / environment / seed / generator
  yapworld.unload:
    default: op
  yapworld.teleport:
    default: op
  yapworld.selection:
    default: op
    description: Wand, pos1/pos2, in-game GUI, fill/walls/shell
  yapworld.editor:
    default: op
    description: Open the browser world edit studio (/yapworld editor)
  yapworld.schematic:
    default: op
  yapworld.cui:
    default: op
    description: Receive WorldEditCUI selection outlines
  yapworld.brush:
    default: op
  yapworld.pregen:
    default: op
