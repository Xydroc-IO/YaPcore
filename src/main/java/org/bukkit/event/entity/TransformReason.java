package org.bukkit.event.entity;

/** Paper API stub (YaPcore compatibility). */
public enum TransformReason {
    CURED, FROZEN, INFECTION, DROWNED, SHEARED, LIGHTNING, SPLIT, PIGLIN_ZOMBIFIED, METAMORPHOSIS, UNKNOWN;
}
