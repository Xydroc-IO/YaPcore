package org.bukkit.event.entity;

/** Paper API stub (YaPcore compatibility). */
public enum Cause {
    DEATH, DESPAWN, DROP, ENTER_BLOCK, EXPLODE, HIT, MERGE, OUT_OF_WORLD, PICKUP, PLAYER_QUIT, PLUGIN, TRANSFORMATION, UNLOAD, DISCARD;
}
